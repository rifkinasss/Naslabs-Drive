; Keep PHP's transport limits aligned with Cloud NL's system upload policy.
; For production, configure these values in PHP-FPM/hosting instead.
upload_max_filesize=512M
post_max_size=520M
max_file_uploads=100
